<Window
    x:Class="GreenLuma_Manager.Dialogs.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Settings"
    Width="950"
    Height="680"
    MinWidth="850"
    MinHeight="930"
    Background="#FF1A1A1A"
    Foreground="#FFF"
    ResizeMode="CanResize"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="40"
            CornerRadius="0"
            GlassFrameThickness="1"
            ResizeBorderThickness="4"
            UseAeroCaptionButtons="True" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>

            <CubicEase x:Key="DefaultEase" EasingMode="EaseOut" />


            <Style x:Key="SettingsComboBox" TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource BackgroundDark}" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Height" Value="44" />
                <Setter Property="Padding" Value="16,0" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton
                                    x:Name="ToggleButton"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    ClickMode="Press"
                                    Focusable="False"
                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border
                                                x:Name="BorderElement"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                                <Border.BorderBrush>
                                                    <SolidColorBrush x:Name="BorderBrushRef" Color="#FF2A2A2A" />
                                                </Border.BorderBrush>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="36" />
                                                    </Grid.ColumnDefinitions>
                                                    <Path
                                                        x:Name="Arrow"
                                                        Grid.Column="1"
                                                        Width="11"
                                                        Height="11"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M7 10l5 5 5-5z"
                                                        RenderTransformOrigin="0.5,0.5"
                                                        Stretch="Uniform">
                                                        <Path.Fill>
                                                            <SolidColorBrush x:Name="ArrowBrush" Color="#FFAAAAAA" />
                                                        </Path.Fill>
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="0" />
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="Arrow"
                                                                    Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                                    To="180"
                                                                    Duration="0:0:0.25">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CubicEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="BorderBrushRef"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FFFFFFFF"
                                                                    Duration="0:0:0.2" />
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="ArrowBrush"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FFFFFFFF"
                                                                    Duration="0:0:0.2" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="Arrow"
                                                                    Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                                    To="0"
                                                                    Duration="0:0:0.25">
                                                                    <DoubleAnimation.EasingFunction>
                                                                        <CubicEase EasingMode="EaseOut" />
                                                                    </DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="BorderBrushRef"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FF2A2A2A"
                                                                    Duration="0:0:0.2" />
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="ArrowBrush"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FFAAAAAA"
                                                                    Duration="0:0:0.2" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="BorderBrushRef"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FFAAAAAA"
                                                                    Duration="0:0:0.15" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <ColorAnimation
                                                                    Storyboard.TargetName="BorderBrushRef"
                                                                    Storyboard.TargetProperty="Color"
                                                                    To="#FF2A2A2A"
                                                                    Duration="0:0:0.15" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter
                                    x:Name="ContentSite"
                                    Margin="16,0,36,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    IsHitTestVisible="False" />
                                <Popup
                                    x:Name="Popup"
                                    AllowsTransparency="True"
                                    Focusable="False"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    Placement="Bottom"
                                    PopupAnimation="None">
                                    <Border
                                        x:Name="DropDownBorder"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Margin="0,4,0,0"
                                        Padding="4"
                                        Background="{StaticResource BackgroundSurface}"
                                        BorderBrush="{StaticResource Border}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        RenderTransformOrigin="0.5,0">
                                        <Border.Effect>
                                            <DropShadowEffect
                                                BlurRadius="16"
                                                Direction="270"
                                                Opacity="0.4"
                                                ShadowDepth="8"
                                                Color="Black" />
                                        </Border.Effect>
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>
                                        <ScrollViewer
                                            MaxHeight="200"
                                            HorizontalScrollBarVisibility="Disabled"
                                            VerticalScrollBarVisibility="Auto">
                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetName="DropDownBorder"
                                                    Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                    From="0.9"
                                                    To="1"
                                                    Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="Padding" Value="12,10" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border
                                x:Name="ItemBorder"
                                Margin="2,1"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    SnapsToDevicePixels="True" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="IsSelected" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FF2A2A2A" />
                                    <Setter Property="Cursor" Value="Hand" />
                                </MultiTrigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FF3A3A3A" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="StandardButton" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="{StaticResource BackgroundSurface}" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="true">
                                <ContentPresenter
                                    x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Focusable="False"
                                    RecognizesAccessKey="True"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF252525" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF444444" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF151515" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style
                x:Key="AccentButton"
                BasedOn="{StaticResource StandardButton}"
                TargetType="{x:Type Button}">
                <Setter Property="Background" Value="{StaticResource Text}" />
                <Setter Property="Foreground" Value="{StaticResource BackgroundDark}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                                <ContentPresenter
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.7" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="RoundedProgressBar" TargetType="{x:Type ProgressBar}">
                <Setter Property="Background" Value="{StaticResource Border}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="6" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ProgressBar}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="3">
                                <Grid x:Name="PART_Track">
                                    <Rectangle
                                        x:Name="PART_Indicator"
                                        Fill="{TemplateBinding Foreground}"
                                        HorizontalAlignment="Left"
                                        RadiusX="3"
                                        RadiusY="3" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Setter TargetName="PART_Indicator" Property="Width" Value="60" />
                                    <Setter TargetName="PART_Indicator" Property="RenderTransformOrigin" Value="0.5,0.5" />
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimationUsingKeyFrames
                                                    Storyboard.TargetName="PART_Indicator"
                                                    Storyboard.TargetProperty="Opacity">
                                                    <SplineDoubleKeyFrame KeyTime="0:0:0" Value="0.4" />
                                                    <SplineDoubleKeyFrame KeyTime="0:0:0.8" Value="1" />
                                                    <SplineDoubleKeyFrame KeyTime="0:0:1.6" Value="0.4" />
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style
                x:Key="DangerButton"
                BasedOn="{StaticResource StandardButton}"
                TargetType="{x:Type Button}">
                <Setter Property="Foreground" Value="{StaticResource Danger}" />
                <Setter Property="BorderBrush" Value="{StaticResource Danger}" />
                <Setter Property="Background" Value="#11FF4444" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource Danger}" />
                        <Setter Property="Foreground" Value="{StaticResource Text}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="InputTextBox" TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="{StaticResource BackgroundSurface}" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="12,10" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Height" Value="44" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="CaretBrush" Value="{StaticResource Text}" />
                <Setter Property="SelectionBrush" Value="{StaticResource TextSecondary}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                                <Grid>
                                    <ScrollViewer
                                        x:Name="PART_ContentHost"
                                        Focusable="false"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#FFFFFFFF"
                                                    Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#FF2A2A2A"
                                                    Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF555555" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#080808" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF151515" />
                                    <Setter Property="Foreground" Value="#FF555555" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="InputPasswordBox" TargetType="{x:Type PasswordBox}">
                <Setter Property="Background" Value="{StaticResource BackgroundSurface}" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="12,10" />
                <Setter Property="FontSize" Value="14" />
                                <Setter Property="Height" Value="44" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="CaretBrush" Value="{StaticResource Text}" />
                <Setter Property="SelectionBrush" Value="{StaticResource TextSecondary}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type PasswordBox}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                                <Grid>
                                    <ScrollViewer
                                        x:Name="PART_ContentHost"
                                        Focusable="false"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#FFFFFFFF"
                                                    Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    Storyboard.TargetName="border"
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="#FF2A2A2A"
                                                    Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF555555" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ToggleSwitch" TargetType="{x:Type CheckBox}">
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid Margin="0,0,0,0" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Content}"
                                    TextBlock.FontSize="14"
                                    TextBlock.FontWeight="Medium" />
                                <Border
                                    x:Name="Track"
                                    Grid.Column="1"
                                    Width="48"
                                    Height="26"
                                    Background="{StaticResource Border}"
                                    CornerRadius="13">
                                    <Grid>
                                        <Ellipse
                                            x:Name="Knob"
                                            Width="20"
                                            Height="20"
                                            Margin="3,0,0,0"
                                            HorizontalAlignment="Left"
                                            Fill="{StaticResource TextSecondary}">
                                            <Ellipse.RenderTransform>
                                                <TranslateTransform X="0" />
                                            </Ellipse.RenderTransform>
                                        </Ellipse>
                                    </Grid>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="Track"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#FFFFFFFF"
                                                    Duration="0:0:0.4" />
                                                <ColorAnimation
                                                    Storyboard.TargetName="Knob"
                                                    Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                                    To="#FF1A1A1A"
                                                    Duration="0:0:0.3" />
                                                <DoubleAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="Knob"
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                    To="22"
                                                    Duration="0:0:0.4" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation
                                                    Storyboard.TargetName="Track"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#FF2A2A2A"
                                                    Duration="0:0:0.4" />
                                                <ColorAnimation
                                                    Storyboard.TargetName="Knob"
                                                    Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                                    To="#FFAAAAAA"
                                                    Duration="0:0:0.3" />
                                                <DoubleAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="Knob"
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                    To="0"
                                                    Duration="0:0:0.4" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="NavRadioButton" TargetType="{x:Type RadioButton}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Padding" Value="24,14" />
                <Setter Property="Margin" Value="0,0,0,4" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="3" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border
                                        x:Name="indicator"
                                        Width="3"
                                        Height="20"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Background="{StaticResource Text}"
                                        CornerRadius="1.5"
                                        Opacity="0"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleY="0" />
                                        </Border.RenderTransform>
                                    </Border>
                                    <ContentPresenter
                                        Grid.Column="1"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource Text}" />
                                    <Setter Property="Background" Value="#0FFFFFFF" />
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetName="indicator"
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="1"
                                                    Duration="0:0:0.3" />
                                                <DoubleAnimation
                                                    EasingFunction="{StaticResource DefaultEase}"
                                                    Storyboard.TargetName="indicator"
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                    To="1"
                                                    Duration="0:0:0.4" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetName="indicator"
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="0"
                                                    Duration="0:0:0.2" />
                                                <DoubleAnimation
                                                    Storyboard.TargetName="indicator"
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                    To="0"
                                                    Duration="0:0:0.3" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource Text}" />
                                    <Setter TargetName="border" Property="Background" Value="#08FFFFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FadePanel" TargetType="Grid">
                <Style.Triggers>
                    <Trigger Property="Visibility" Value="Visible">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Opacity"
                                        From="0"
                                        To="1"
                                        Duration="0:0:0.5" />
                                    <DoubleAnimation
                                        EasingFunction="{StaticResource DefaultEase}"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                        From="10"
                                        To="0"
                                        Duration="0:0:0.5" />
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="DescriptionText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{StaticResource TextTertiary}" />
                <Setter Property="Margin" Value="0,0,0,12" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <Window.RenderTransform>
        <TranslateTransform />
    </Window.RenderTransform>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.Column="0"
            Background="{StaticResource BackgroundSurface}"
            BorderBrush="{StaticResource Border}"
            BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="30,40,30,40">
                    <TextBlock
                        FontSize="22"
                        FontWeight="Bold"
                        Foreground="{StaticResource Text}"
                        Text="Settings" />
                    <TextBlock
                        Margin="0,6,0,0"
                        FontSize="13"
                        Foreground="{StaticResource TextTertiary}"
                        Text="Preferences &amp; Config" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="16,0">
                    <RadioButton
                        x:Name="NavGeneral"
                        Checked="Nav_Checked"
                        Content="General"
                        GroupName="Nav"
                        IsChecked="True"
                        Style="{StaticResource NavRadioButton}" />
                    <RadioButton
                        x:Name="NavSystem"
                        Checked="Nav_Checked"
                        Content="System"
                        GroupName="Nav"
                        Style="{StaticResource NavRadioButton}" />
                    <RadioButton
                        x:Name="NavAdvanced"
                        Checked="Nav_Checked"
                        Content="Advanced"
                        GroupName="Nav"
                        Style="{StaticResource NavRadioButton}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="24,30">
                    <Button
                        Click="Ok_Click"
                        Content="Save Changes"
                        Style="{StaticResource AccentButton}" />
                    <Button
                        Margin="0,12,0,0"
                        Click="Cancel_Click"
                        Content="Discard &amp; Close"
                        FontSize="14"
                        Style="{StaticResource StandardButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="{StaticResource BackgroundDark}">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" />

            <Grid Grid.Row="1" Margin="40,0,40,40">

                <Grid
                    x:Name="ViewGeneral"
                    RenderTransformOrigin="0.5,0.5"
                    Style="{StaticResource FadePanel}"
                    Visibility="Visible">
                    <Grid.RenderTransform>
                        <TranslateTransform />
                    </Grid.RenderTransform>

                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,4"
                            FontSize="24"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource Text}"
                            Text="General" />
                        <TextBlock
                            Margin="0,0,0,24"
                            FontSize="14"
                            Foreground="{StaticResource TextSecondary}"
                            Text="Configure your installation paths." />

                        <Border
                            Padding="24"
                            Background="{StaticResource BackgroundSurface}"
                            BorderBrush="{StaticResource Border}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <StackPanel>
                                <Grid Margin="0,0,0,20">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        FontWeight="Bold"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="INSTALLATION PATHS" />
                                    <Button
                                        HorizontalAlignment="Right"
                                        Click="AutoDetect_Click"
                                        Content="Auto-Detect"
                                        FontSize="11"
                                        Style="{StaticResource StandardButton}" />
                                </Grid>

                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock
                                        Margin="0,0,0,4"
                                        FontSize="13"
                                        Foreground="{StaticResource TextSecondary}"
                                        Text="Steam Directory" />
                                    <TextBlock Style="{StaticResource DescriptionText}"
                                               Text="The main installation folder containing Steam.exe." />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtSteamPath" Style="{StaticResource InputTextBox}" />
                                        <Button
                                            Grid.Column="2"
                                            Width="80"
                                            Click="BrowseSteam_Click"
                                            Content="Browse"
                                            Style="{StaticResource StandardButton}" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel x:Name="PanelGreenLumaDir">
                                    <TextBlock
                                        Margin="0,0,0,4"
                                        FontSize="13"
                                        Foreground="{StaticResource TextSecondary}"
                                        Text="GreenLuma Directory" />
                                    <TextBlock x:Name="TxtGreenLumaDesc" Style="{StaticResource DescriptionText}"
                                               Text="The folder where DLLInjector.exe and GreenLuma files are located." />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtGreenLumaPath" Style="{StaticResource InputTextBox}" />
                                        <Button
                                            x:Name="BtnBrowseGreenLuma"
                                            Grid.Column="2"
                                            Width="80"
                                            Click="BrowseGreenLuma_Click"
                                            Content="Browse"
                                            Style="{StaticResource StandardButton}" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel Margin="0,20,0,0">
                                    <TextBlock
                                        Margin="0,0,0,4"
                                        FontSize="13"
                                        Foreground="{StaticResource TextSecondary}"
                                        Text="GreenLuma Mode" />
                                    <TextBlock Style="{StaticResource DescriptionText}"
                                               Text="Choose which installation mode to use when multiple are detected." />
                                    <ComboBox
                                        x:Name="CmbGreenLumaMode"
                                        Style="{StaticResource SettingsComboBox}"
                                        SelectionChanged="GreenLumaMode_SelectionChanged">
                                        <ComboBoxItem Tag="">Auto-Detect</ComboBoxItem>
                                        <ComboBoxItem Tag="Normal">Normal</ComboBoxItem>
                                        <ComboBoxItem Tag="StealthAny">StealthAny</ComboBoxItem>
                                        <ComboBoxItem Tag="User32">User32</ComboBoxItem>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid
                    x:Name="ViewSystem"
                    RenderTransformOrigin="0.5,0.5"
                    Style="{StaticResource FadePanel}"
                    Visibility="Collapsed">
                    <Grid.RenderTransform>
                        <TranslateTransform />
                    </Grid.RenderTransform>

                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,4"
                            FontSize="24"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource Text}"
                            Text="System" />
                        <TextBlock
                            Margin="0,0,0,24"
                            FontSize="14"
                            Foreground="{StaticResource TextSecondary}"
                            Text="Manage application behavior and updates." />

                        <Border
                            Margin="0,0,0,20"
                            Padding="24"
                            Background="{StaticResource BackgroundSurface}"
                            BorderBrush="{StaticResource Border}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <StackPanel>
                                <TextBlock
                                    Margin="0,0,0,20"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Foreground="{StaticResource TextTertiary}"
                                    Text="STARTUP" />
                                <StackPanel Margin="0,0,0,24">
                                    <CheckBox
                                        x:Name="ChkReplaceSteamAutostart"
                                        Content="Replace Steam on Windows Startup"
                                        Style="{StaticResource ToggleSwitch}" />
                                    <TextBlock
                                        Margin="0,4,60,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="Launch GreenLuma Manager automatically when Windows starts, replacing the default Steam startup."
                                        TextWrapping="Wrap" />
                                </StackPanel>

                                <StackPanel>
                                    <CheckBox
                                        x:Name="ChkPrefetchAppList"
                                        Content="Prefetch Steam App List"
                                        Style="{StaticResource ToggleSwitch}" />
                                    <TextBlock
                                        Margin="0,4,60,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="Speed up the App Search by prefetching all Steam Apps when GreenLuma Manager starts."
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border
                            Padding="24"
                            Background="{StaticResource BackgroundSurface}"
                            BorderBrush="{StaticResource Border}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <StackPanel>
                                <TextBlock
                                    Margin="0,0,0,20"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Foreground="{StaticResource TextTertiary}"
                                    Text="UPDATES" />
                                <StackPanel Margin="0,0,0,24">
                                    <CheckBox
                                        x:Name="ChkDisableUpdateCheck"
                                        Checked="DisableUpdateCheck_Changed"
                                        Content="Disable Update Checking"
                                        Style="{StaticResource ToggleSwitch}"
                                        Unchecked="DisableUpdateCheck_Changed" />
                                    <TextBlock
                                        Margin="0,4,60,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="Prevent the application from checking for new versions on startup."
                                        TextWrapping="Wrap" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,24">
                                    <CheckBox
                                        x:Name="ChkAutoUpdate"
                                        Content="Auto-Install Updates"
                                        Style="{StaticResource ToggleSwitch}" />
                                    <TextBlock
                                        Margin="0,4,60,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="Download and install updates automatically instead of being redirected to the GitHub page."
                                        TextWrapping="Wrap" />
                                </StackPanel>

                                <StackPanel>
                                    <CheckBox
                                        x:Name="ChkCheckGreenLumaUpdates"
                                        Content="Check for GreenLuma Updates"
                                        Style="{StaticResource ToggleSwitch}" />
                                    <TextBlock
                                        Margin="0,4,60,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextTertiary}"
                                        Text="Notify when a newer version of GreenLuma is available on cs.rin.ru."
                                        TextWrapping="Wrap" />
                                    <Grid Margin="0,16,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="8" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Row="0"
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            Foreground="{StaticResource TextSecondary}"
                                            Text="Username:" />
                                        <TextBox
                                            Grid.Row="0"
                                            Grid.Column="2"
                                            x:Name="TxtGreenLumaUsername"
                                            Style="{StaticResource InputTextBox}"
                                            TextChanged="GreenLumaCredential_Changed" />
                                        <TextBlock
                                            Grid.Row="2"
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            Foreground="{StaticResource TextSecondary}"
                                            Text="Password:" />
                                        <PasswordBox
                                            Grid.Row="2"
                                            Grid.Column="2"
                                            x:Name="PwdGreenLumaPassword"
                                            Style="{StaticResource InputPasswordBox}"
                                            PasswordChanged="GreenLumaCredential_Changed" />
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                        <Button
                                            x:Name="BtnCheckGreenLumaUpdates"
                                            Content="Check Now"
                                            Style="{StaticResource StandardButton}"
                                            Padding="12,6"
                                            Click="CheckGreenLumaUpdates_Click" />
                                        <Button
                                            x:Name="BtnDownloadGreenLuma"
                                            Content="Download &amp; Deploy Latest"
                                            Style="{StaticResource StandardButton}"
                                            Margin="12,0,0,0"
                                            Padding="12,6"
                                            Click="DownloadGreenLuma_Click" />
                                    </StackPanel>

                                    <StackPanel
                                        x:Name="PnlDownloadProgress"
                                        Margin="0,16,0,0"
                                        Visibility="Collapsed">
                                        <ProgressBar
                                            x:Name="DownloadProgressBar"
                                            Height="6"
                                            Style="{StaticResource RoundedProgressBar}"
                                            Value="0" />
                                        <TextBlock
                                            x:Name="TxtDownloadStatus"
                                            Margin="0,8,0,0"
                                            FontSize="12"
                                            Foreground="{StaticResource TextSecondary}"
                                            Text="Starting..." />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid
                    x:Name="ViewAdvanced"
                    RenderTransformOrigin="0.5,0.5"
                    Style="{StaticResource FadePanel}"
                    Visibility="Collapsed">
                    <Grid.RenderTransform>
                        <TranslateTransform />
                    </Grid.RenderTransform>

                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,4"
                            FontSize="24"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource Text}"
                            Text="Advanced" />
                        <TextBlock
                            Margin="0,0,0,24"
                            FontSize="14"
                            Foreground="{StaticResource TextSecondary}"
                            Text="Dangerous actions and data management." />

                        <Border
                            Padding="24"
                            Background="#0DFF4444"
                            BorderBrush="{StaticResource Danger}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="20"
                                    Opacity="0.15"
                                    ShadowDepth="0"
                                    Color="#FFFF4444" />
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="15"
                                        FontWeight="Bold"
                                        Foreground="{StaticResource Danger}"
                                        Text="Danger Zone" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        FontSize="13"
                                        Foreground="{StaticResource TextSecondary}"
                                        Text="Irreversibly wipe all application data, profiles, settings, and cache."
                                        TextWrapping="Wrap" />
                                </StackPanel>
                                <Button
                                    Grid.Column="1"
                                    Click="WipeData_Click"
                                    Content="Wipe Data"
                                    Style="{StaticResource DangerButton}" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

            </Grid>
        </Grid>
    </Grid>
</Window>